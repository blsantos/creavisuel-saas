# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# URL de votre instance Supabase
VITE_SUPABASE_URL=https://votre-projet.supabase.co
SUPABASE_URL=https://votre-projet.supabase.co

# Clé publique Supabase (safe pour le client)
# Trouvable dans: Supabase Dashboard > Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=votre-cle-anon-publique
SUPABASE_ANON_KEY=votre-cle-anon-publique

# ⚠️ CRITIQUE - Clé service role (JAMAIS exposer au client)
# Utilisé uniquement dans les scripts backend Node.js
# Trouvable dans: Supabase Dashboard > Settings > API > Project API keys > service_role secret
SUPABASE_SERVICE_ROLE_KEY=votre-service-role-key-ici

# =============================================================================
# BASEROW CONFIGURATION (optionnel)
# =============================================================================
# URL de votre instance Baserow
BASEROW_URL=https://baserow.lecoach.digital

# Token d'API Baserow pour l'import de données
# Trouvable dans: Baserow > Settings > API tokens
BASEROW_TOKEN=votre-baserow-token-ici

# =============================================================================
# N8N CONFIGURATION
# =============================================================================
# URL webhook N8N (optionnel - peut être configuré par tenant dans la DB)
VITE_DEFAULT_WEBHOOK_URL=https://votre-n8n.com/webhook/votre-id/chat

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
VITE_APP_NAME=CréaVisuel SaaS
VITE_APP_URL=https://votre-domaine.com

# =============================================================================
# CLOUDINARY (optionnel)
# =============================================================================
VITE_CLOUDINARY_CLOUD_NAME=votre-cloud-name
VITE_CLOUDINARY_API_KEY=votre-api-key

# =============================================================================
# DEVELOPMENT
# =============================================================================
NODE_ENV=production
PORT=3000

# =============================================================================
# SÉCURITÉ - IMPORTANT
# =============================================================================
# ⚠️ Ne JAMAIS commiter le fichier .env dans git
# ⚠️ Ajouter .env dans .gitignore
# ⚠️ Les variables VITE_* sont exposées au client (frontend)
# ⚠️ Les variables sans VITE_ sont pour le backend uniquement
# ⚠️ Rotation régulière des clés sensibles (SERVICE_ROLE_KEY)
